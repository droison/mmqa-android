<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/native_camera_relativeLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".NativeCameraActivity" >

    <Button 
        android:id="@+id/highquality"
        android:layout_height="wrap_content"
        
        android:text="@string/native_camera_high"
        android:layout_width="fill_parent"/>
    <Button 
        android:id="@+id/lowquality"
        android:layout_height="wrap_content"
        android:layout_below="@id/highquality"
        android:text="@string/native_camera_low"
        android:layout_width="fill_parent"/>

</RelativeLayout>